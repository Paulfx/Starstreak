#ifndef PARTIE_H
#define PARTIE_H

#include <stdlib.h>
#include <iostream>
#include <stdio.h>
#include <assert.h>
#include <partition.h>
#include <jeu.h>
#include <score.h>

/**
 @brief la classe qui teste le succés d'une note et modifie le score
 */

class Terrein {
    
private:
    Partition part;
    line tab[8];
    float score;
    
    
public:
    Terrein();
    
    Terrein(   );
};
